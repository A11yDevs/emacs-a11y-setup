# Repository confirmation: A11yDevs/emacs-a11y-setup

Evidência gerada automaticamente para a tarefa T032 — confirmação do monorepo
e árvore `lisp/`.

Remote repository URL:

```
https://github.com/A11yDevs/emacs-a11y-setup
```

Conteúdo atual da árvore `lisp/` (listagem recursiva):

```
a11y-hello
emacs-a11y-setup-community-packages.el
emacs-a11y-setup-community-packages.el.bak
emacs-a11y-setup-doctor.el
emacs-a11y-setup-handoff.el
emacs-a11y-setup-modules.el
emacs-a11y-setup-profiles.el
emacs-a11y-setup-workspace.el

lisp/a11y-hello:
a11y-hello.el README.md
```

Observação: Este arquivo serve como evidência para auditores e para linkagem
em relatórios/PRs que implementam mudanças que afetam `lisp/`.
